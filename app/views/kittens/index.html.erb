<h2>Kittens</h2>

<%= link_to "Create Kitten", new_kitten_path%>
<% @kittens.each do |kitten| %>
    <div class="kitten">
        <h3> Name: <%= kitten.name %></h3>
        <%= link_to "View Kitten", kitten_path(kitten) %>
    </div>
<% end %>